spring:
  application:
    name: front
  cloud:
    consul:
      host: localhost          # ????? Consul (?? ????????? localhost)
      port: 8500               # ???? Consul (8500 ?? ?????????)
      discovery:
        service-name: front  # ?????????????, ?? ????????? ??????? spring.application.name
      config:
        enabled: true            # ???????? Consul Config ??? ??????? ????????????
        prefix: config           # ??????? ? KV-????????? Consul (?? ????????? "config")
        defaultContext: application  # ???????? ?? ?????????
server:
  port: 8083

